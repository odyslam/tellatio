# Telegram API credentials — get from https://my.telegram.org/apps
TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# Session string — run: pnpm login:telegram
TELEGRAM_SESSION=

# Attio API key — get from Attio workspace settings
ATTIO_API_KEY=

# Optional: Telegram folder name to sync (default: "Attio")
TELEGRAM_FOLDER_NAME=Attio

# Optional: Sync interval in minutes (default: 15)
SYNC_INTERVAL_MINUTES=15

# Optional: Directory for sync state file (default: /data)
DATA_DIR=/data
